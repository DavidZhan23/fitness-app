# 复制为 server/.env 后修改（本地开发）
# 先启动数据库: cd deploy && docker compose up -d postgres

DATABASE_URL=postgres://fitness:change_me_strong_password@localhost:5432/fitness
JWT_SECRET=change_me_jwt_secret_min_32_chars_local_dev
REGISTRATION_KEY=454676
PORT=3001
CORS_ORIGIN=http://localhost:5173
